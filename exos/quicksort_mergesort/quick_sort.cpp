#include "quick_sort.hh"

/*
*  ============================= LINKED LISTS =============================
*/

// std::unique_ptr<LinkedList> quick_sort(std::unique_ptr<LinkedList> list) {
//     if (list.empty() || list->size == 1) {
//         return list;
//     }

//     auto pivot = list->last->value;
//     auto left = std::make_unique<LinkedList>();
//     auto right = std::make_unique<LinkedList>();

//     for (auto current = list->first.get(); current != nullptr; current = current->next.get())
// }